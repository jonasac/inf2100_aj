TODO

	innlevering [x]
	fikse bug med feil linje ved ugjenkjennelig symbol [_]

    fikse alle tre-utskrift-metodene


    part1

        * Log.java
            * enterParser()
            * leaveParser()
            * indentTree()
            * outdentTree()
        * Syntax.java
            * init()
            * finish()
            * DeclList()
            * printTree()
            * addDecl()
            * class GlobalDeclList()
            * class LocalDeclList() => parse()
